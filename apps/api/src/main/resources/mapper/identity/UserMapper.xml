<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.today.identity.UserMapper">

  <resultMap id="UserEntityMap" type="com.today.persistence.UserEntity">
    <id property="id" column="id"/>
    <result property="email" column="email"/>
    <result property="displayName" column="display_name"/>
    <result property="passwordHash" column="password_hash"/>
    <result property="createdAt" column="created_at"/>
    <result property="updatedAt" column="updated_at"/>
  </resultMap>

  <select id="findById" resultMap="UserEntityMap">
    SELECT id, email, display_name, password_hash, created_at, updated_at
    FROM users
    WHERE id = #{id}
    LIMIT 1
  </select>

  <select id="findByEmail" resultMap="UserEntityMap">
    SELECT id, email, display_name, password_hash, created_at, updated_at
    FROM users
    WHERE email = #{email}
    LIMIT 1
  </select>

  <insert id="insert">
    INSERT INTO users (
      id, email, display_name, password_hash, created_at, updated_at
    ) VALUES (
      #{id}, #{email}, #{displayName}, #{passwordHash}, #{createdAt}, #{updatedAt}
    )
  </insert>
</mapper>
