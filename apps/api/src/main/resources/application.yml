server:
  port: ${PORT:3001}
  # Listen on all interfaces so other Compose services can reach the API.
  address: ${SERVER_ADDRESS:0.0.0.0}

spring:
  application:
    name: today-api
  datasource:
    url: ${MYSQL_URL:jdbc:mysql://localhost:3306/today?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=utf8}
    username: ${MYSQL_USER:today}
    password: ${MYSQL_PASSWORD:today}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-timeout: 30000
      # Allow MySQL a bit longer after docker healthcheck (auth/SQL ready).
      initialization-fail-timeout: 120000
      maximum-pool-size: 10

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.today.persistence
  configuration:
    map-underscore-to-camel-case: true

today:
  schema:
    # Apply classpath db/schema.sql + migrations on boot when tables are missing.
    bootstrap: ${TODAY_SCHEMA_BOOTSTRAP:true}
  jwt:
    secret: ${TODAY_JWT_SECRET:today-dev-jwt-secret-change-me-please-32bytes}
    expire-hours: ${TODAY_JWT_EXPIRE_HOURS:168}
  ai:
    api-key: ${OPENAI_API_KEY:}
    base-url: ${OPENAI_BASE_URL:https://api.openai.com/v1}
    model: ${OPENAI_MODEL:gpt-4o-mini}
    embedding-model: ${OPENAI_EMBEDDING_MODEL:text-embedding-3-small}
    timeout-ms: ${OPENAI_TIMEOUT_MS:30000}
    json-response-format: ${OPENAI_JSON_RESPONSE_FORMAT:true}
    temperature: ${OPENAI_TEMPERATURE:0.3}
    retrieve-top-k: ${OPENAI_RETRIEVE_TOP_K:5}
    async-checkin: ${TODAY_AI_ASYNC_CHECKIN:true}
    job-poll-ms: ${TODAY_AI_JOB_POLL_MS:15000}
    admin-token: ${TODAY_AI_ADMIN_TOKEN:}
    log-retain-days: ${TODAY_AI_LOG_RETAIN_DAYS:7}
  vector:
    # mysql = 扫描 embedding_json；qdrant = 外挂向量库（失败回退 mysql）
    provider: ${TODAY_VECTOR_PROVIDER:mysql}
    qdrant-url: ${TODAY_QDRANT_URL:http://127.0.0.1:6333}
    collection: ${TODAY_QDRANT_COLLECTION:today_memories}
    dimensions: ${TODAY_VECTOR_DIMENSIONS:1536}
    api-key: ${TODAY_QDRANT_API_KEY:}
    timeout-ms: ${TODAY_QDRANT_TIMEOUT_MS:5000}
    admin-token: ${TODAY_VECTOR_ADMIN_TOKEN:}
